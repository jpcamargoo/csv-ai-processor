# 🚀 CSV + IA Processor v2.1 - Variáveis de Ambiente

# Chave da API OpenAI (opcional - funciona sem ela)
OPENAI_API_KEY=sua_chave_openai_aqui de ambiente para a aplicação CSV + IA

# Chave da API OpenAI (obrigatório)
OPENAI_API_KEY=sua_chave_aqui

# Porta do servidor (opcional, padrão: 3000)
PORT=3000

# Configurações da IA (opcionais)
AI_MODEL=gpt-3.5-turbo
AI_MAX_TOKENS=500
AI_TEMPERATURE=0.7
